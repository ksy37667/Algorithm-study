#include <iostream>

using namespace std;

int N;
string stri;

string func(string stri, string str){
	
}

int main(){
	ios::sync_with_stdio(false);
    cin.tie(0); cout.tie(0);
    
	int T;
	cin >> T;
	
	for(int test = 0; test = T; test++){
		string str;
		cin >> str;
		cin >> N;
		
		for(int i = 0; i<N; i++){
			cin >> stri;
			func(stri,str);
		}
	}
}
